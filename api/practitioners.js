module.exports = (req, res) => {\n  res.setHeader('Access-Control-Allow-Credentials', true);\n  res.setHeader('Access-Control-Allow-Origin', '*');\n  res.setHeader('Access-Control-Allow-Methods', 'GET,OPTIONS,PATCH,DELETE,POST,PUT');\n  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');\n\n  if (req.method === 'OPTIONS') {\n    res.status(200).end();\n    return;\n  }\n\n  const { url, method } = req;\n\n  // Get all practitioners\n  if (method === 'GET' && !url.includes('/available-slots') && !url.includes('/dashboard')) {\n    return res.status(200).json({\n      success: true,\n      data: {\n        practitioners: [\n          {\n            _id: '1',\n            name: 'Dr. Priya Sharma',\n            specialization: 'Panchakarma Specialist',\n            consultationFee: 1500,\n            experience: '15 years',\n            stats: { avgRating: 4.8, appointmentCount: 150 }\n          },\n          {\n            _id: '2',\n            name: 'Dr. Rajesh Kumar',\n            specialization: 'Ayurvedic Medicine',\n            consultationFee: 1200,\n            experience: '12 years',\n            stats: { avgRating: 4.6, appointmentCount: 120 }\n          }\n        ]\n      }\n    });\n  }\n\n  // Get available slots\n  if (url.includes('/available-slots') && method === 'GET') {\n    return res.status(200).json({\n      success: true,\n      data: {\n        availableSlots: ['09:00 AM', '10:00 AM', '11:00 AM', '02:00 PM', '03:00 PM', '04:00 PM']\n      }\n    });\n  }\n\n  // Dashboard stats\n  if (url.includes('/dashboard') && method === 'GET') {\n    return res.status(200).json({\n      success: true,\n      data: {\n        stats: {\n          todayBookings: 5,\n          totalPatients: 45,\n          avgRating: 4.7,\n          monthlyRevenue: 25000\n        }\n      }\n    });\n  }\n\n  res.status(404).json({ success: false, message: 'Practitioner endpoint not found' });\n};